<div class="col-sm-6">
<div class="div-pad"> Departure </div>
  <select name="departure_id" class="my-select">
  <% @airports.each do |airport| %>
    <option value="<%= airport.id %>"> <%= airport.name  + " " + airport.location %> </option>
  <% end %>
  </select>
</div>
