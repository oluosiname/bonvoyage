<%= render partial: 'header' %>
<div class="main-bg">
  <div class="container box">
    <div class="row">
      <div class="col s6">
        <div class="row">
          <%= render partial: 'flightinfo' %>
        </div>
      </div>
      <div class="col s6">
      <div class="row header-text">
        <div class="col s5">Name</div>
        <div class="col s2">Email</div>
        <div class="col s3">Phone</div>
        <div class="col s2">cabin</div>
      </div>
      <% @passengers.each do |key, passenger| %>
        <div class="row ">
          <div class="col s5"><%= passenger["first_name"] + " " + passenger["last_name"] %></div>
          <div class="col s2"><%= passenger["email"] %></div>
          <div class="col s3"><%= passenger["phone"] %></div>
          <div class="col s2"><%= passenger["cabin"] %></div>
        </div>
      <% end %>
      <%= form_tag %>
      <div class="row"> <%= submit_tag "Confirm", class: "btn black"%></div>
      <%= @current_booking %>
      </div>
    </div>
  </div>
</div>ß
